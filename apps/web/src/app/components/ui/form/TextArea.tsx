import styled from 'styled-components';

export const TextArea = styled.textarea`
  width: 100%;
  min-height: 65px;
  padding: 1rem 0.8rem;
  margin: 0.4rem 0;
  resize: none;
  field-sizing: content;
  border-radius: 4px;
`